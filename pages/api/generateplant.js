export default function handler(req, res){
    // 
    // const response = await openai.images.generate({
    //   model: "dall-e-3",
    //   prompt: `

    //   stick to the following rules:
    //   generate a hyper-realistic image. the pot should be of greyish shade and the background should be complete white with no shadows and curves.`,
    //   n: 1,
    //   size: "1024x1024",
    // });
    // const image_url = response.data[0].url;
    // setPlants(image_url);
}